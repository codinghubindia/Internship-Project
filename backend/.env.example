# .env.example
# 
# This file contains example environment variables required to run the backend application.
# Copy this file to `.env` and replace the placeholder values with your actual configuration.
#
# PORT: The port number on which the server will listen.
# MONGO_URI: The MongoDB connection string for your database.
# JWT_SECRET: The secret key used for signing JSON Web Tokens (JWT).

PORT=3000
MONGO_URI=mongodb://localhost:27017/your-db-name
JWT_SECRET=your_jwt_secret_here