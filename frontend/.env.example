# To configure the API endpoint for the frontend, create a `.env` file in the project root.
# Then, add the following environment variable to specify the backend API URL:
# VITE_API_URL=http://localhost:5000/api
VITE_API_URL=http://localhost:5000/api
